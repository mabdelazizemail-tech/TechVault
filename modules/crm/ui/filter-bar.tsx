// The filter bar is a generic list primitive, shared by every module from
// components/ui. Re-exported here so existing CRM imports keep working.
export { FilterBar, type FilterSelect } from "@/components/ui/filter-bar";
